// This file was generated from JSON Schema using quicktype, do not modify it directly.
// To parse the JSON, add this file to your project and do:
//
//   let charactersJSON = try CharactersJSON(json)

import Foundation

// MARK: - CharactersJSON
struct CharactersJSON: Codable {
    let the10000002: The10000002
    let the10000003: The10000003
    let the10000005: The10000005
    let the10000006: The10000006
    let the10000007: The10000007
    let the10000014: The10000014
    let the10000015: The10000015
    let the10000016: The10000016
    let the10000020: The10000020
    let the10000021: The10000021
    let the10000022: The10000022
    let the10000023: The10000023
    let the10000024: The10000024
    let the10000025: The10000025
    let the10000026: The10000026
    let the10000027: The10000027
    let the10000029: The10000029
    let the10000030: The10000030
    let the10000031: The10000031
    let the10000032: The10000032
    let the10000033: The10000033
    let the10000034: The10000034
    let the10000035: The10000035
    let the10000036: The10000036
    let the10000037: The10000037
    let the10000038: The10000038
    let the10000039: The10000039
    let the10000041: The10000041
    let the10000042: The10000042
    let the10000043: The10000043
    let the10000044: The10000044
    let the10000045: The10000045
    let the10000046: The10000046
    let the10000047: The10000047
    let the10000048: The10000048
    let the10000049: The10000049
    let the10000050: The10000050
    let the10000051: The10000051
    let the10000052: The10000052
    let the10000053: The10000053
    let the10000054: The10000054
    let the10000055: The10000055
    let the10000056: The10000056
    let the10000057: The10000057
    let the10000058: The10000058
    let the10000059: The10000059
    let the10000060: The10000060
    let the10000061: The10000061
    let the10000062: The10000062
    let the10000063: The10000063
    let the10000064: The10000064
    let the10000065: The10000065
    let the10000066: The10000066
    let the10000067: The10000067
    let the10000068: The10000068
    let the10000069: The10000069
    let the10000070: The10000070
    let the10000071: The10000071
    let the10000072: The10000072
    let the10000073: The10000073
    let the10000074: The10000074
    let the10000075: The10000075
    let the10000076: The10000076
    let the10000077: The10000077
    let the10000078: The10000078
    let the10000079: The10000079
    let the10000080: The10000080
    let the10000081: The10000081
    let the10000082: The10000082
    let the10000083: The10000083
    let the10000084: The10000084
    let the10000085: The10000085
    let the10000086: The10000086
    let the10000087: The10000087
    let the10000088: The10000088
    let the10000089: The10000089
    let the10000090: The10000090
    let the10000091: The10000091
    let the10000092: The10000092
    let the10000093: The10000093
    let the10000094: The10000094
    let the10000095: The10000095
    let the10000096: The10000096
    let the10000097: The10000097
    let the10000098: The10000098
    let the10000099: The10000099
    let the10000100: The10000100
    let the10000101: The10000101
    let the10000102: The10000102
    let the10000005503: The10000005503
    let the10000005504: The10000005
    let the10000005506: The10000005506
    let the10000005507: The10000005507
    let the10000005508: The10000005508
    let the10000005501: The10000005501
    let the10000007701: The10000007701
    let the10000007703: The10000007703
    let the10000007704: The10000007
    let the10000007706: The10000007706
    let the10000007707: The10000007707
    let the10000007708: The10000007708

    enum CodingKeys: String, CodingKey {
        case the10000002 = "10000002"
        case the10000003 = "10000003"
        case the10000005 = "10000005"
        case the10000006 = "10000006"
        case the10000007 = "10000007"
        case the10000014 = "10000014"
        case the10000015 = "10000015"
        case the10000016 = "10000016"
        case the10000020 = "10000020"
        case the10000021 = "10000021"
        case the10000022 = "10000022"
        case the10000023 = "10000023"
        case the10000024 = "10000024"
        case the10000025 = "10000025"
        case the10000026 = "10000026"
        case the10000027 = "10000027"
        case the10000029 = "10000029"
        case the10000030 = "10000030"
        case the10000031 = "10000031"
        case the10000032 = "10000032"
        case the10000033 = "10000033"
        case the10000034 = "10000034"
        case the10000035 = "10000035"
        case the10000036 = "10000036"
        case the10000037 = "10000037"
        case the10000038 = "10000038"
        case the10000039 = "10000039"
        case the10000041 = "10000041"
        case the10000042 = "10000042"
        case the10000043 = "10000043"
        case the10000044 = "10000044"
        case the10000045 = "10000045"
        case the10000046 = "10000046"
        case the10000047 = "10000047"
        case the10000048 = "10000048"
        case the10000049 = "10000049"
        case the10000050 = "10000050"
        case the10000051 = "10000051"
        case the10000052 = "10000052"
        case the10000053 = "10000053"
        case the10000054 = "10000054"
        case the10000055 = "10000055"
        case the10000056 = "10000056"
        case the10000057 = "10000057"
        case the10000058 = "10000058"
        case the10000059 = "10000059"
        case the10000060 = "10000060"
        case the10000061 = "10000061"
        case the10000062 = "10000062"
        case the10000063 = "10000063"
        case the10000064 = "10000064"
        case the10000065 = "10000065"
        case the10000066 = "10000066"
        case the10000067 = "10000067"
        case the10000068 = "10000068"
        case the10000069 = "10000069"
        case the10000070 = "10000070"
        case the10000071 = "10000071"
        case the10000072 = "10000072"
        case the10000073 = "10000073"
        case the10000074 = "10000074"
        case the10000075 = "10000075"
        case the10000076 = "10000076"
        case the10000077 = "10000077"
        case the10000078 = "10000078"
        case the10000079 = "10000079"
        case the10000080 = "10000080"
        case the10000081 = "10000081"
        case the10000082 = "10000082"
        case the10000083 = "10000083"
        case the10000084 = "10000084"
        case the10000085 = "10000085"
        case the10000086 = "10000086"
        case the10000087 = "10000087"
        case the10000088 = "10000088"
        case the10000089 = "10000089"
        case the10000090 = "10000090"
        case the10000091 = "10000091"
        case the10000092 = "10000092"
        case the10000093 = "10000093"
        case the10000094 = "10000094"
        case the10000095 = "10000095"
        case the10000096 = "10000096"
        case the10000097 = "10000097"
        case the10000098 = "10000098"
        case the10000099 = "10000099"
        case the10000100 = "10000100"
        case the10000101 = "10000101"
        case the10000102 = "10000102"
        case the10000005503 = "10000005-503"
        case the10000005504 = "10000005-504"
        case the10000005506 = "10000005-506"
        case the10000005507 = "10000005-507"
        case the10000005508 = "10000005-508"
        case the10000005501 = "10000005-501"
        case the10000007701 = "10000007-701"
        case the10000007703 = "10000007-703"
        case the10000007704 = "10000007-704"
        case the10000007706 = "10000007-706"
        case the10000007707 = "10000007-707"
        case the10000007708 = "10000007-708"
    }
}

// MARK: CharactersJSON convenience initializers and mutators

extension CharactersJSON {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(CharactersJSON.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the10000002: The10000002? = nil,
        the10000003: The10000003? = nil,
        the10000005: The10000005? = nil,
        the10000006: The10000006? = nil,
        the10000007: The10000007? = nil,
        the10000014: The10000014? = nil,
        the10000015: The10000015? = nil,
        the10000016: The10000016? = nil,
        the10000020: The10000020? = nil,
        the10000021: The10000021? = nil,
        the10000022: The10000022? = nil,
        the10000023: The10000023? = nil,
        the10000024: The10000024? = nil,
        the10000025: The10000025? = nil,
        the10000026: The10000026? = nil,
        the10000027: The10000027? = nil,
        the10000029: The10000029? = nil,
        the10000030: The10000030? = nil,
        the10000031: The10000031? = nil,
        the10000032: The10000032? = nil,
        the10000033: The10000033? = nil,
        the10000034: The10000034? = nil,
        the10000035: The10000035? = nil,
        the10000036: The10000036? = nil,
        the10000037: The10000037? = nil,
        the10000038: The10000038? = nil,
        the10000039: The10000039? = nil,
        the10000041: The10000041? = nil,
        the10000042: The10000042? = nil,
        the10000043: The10000043? = nil,
        the10000044: The10000044? = nil,
        the10000045: The10000045? = nil,
        the10000046: The10000046? = nil,
        the10000047: The10000047? = nil,
        the10000048: The10000048? = nil,
        the10000049: The10000049? = nil,
        the10000050: The10000050? = nil,
        the10000051: The10000051? = nil,
        the10000052: The10000052? = nil,
        the10000053: The10000053? = nil,
        the10000054: The10000054? = nil,
        the10000055: The10000055? = nil,
        the10000056: The10000056? = nil,
        the10000057: The10000057? = nil,
        the10000058: The10000058? = nil,
        the10000059: The10000059? = nil,
        the10000060: The10000060? = nil,
        the10000061: The10000061? = nil,
        the10000062: The10000062? = nil,
        the10000063: The10000063? = nil,
        the10000064: The10000064? = nil,
        the10000065: The10000065? = nil,
        the10000066: The10000066? = nil,
        the10000067: The10000067? = nil,
        the10000068: The10000068? = nil,
        the10000069: The10000069? = nil,
        the10000070: The10000070? = nil,
        the10000071: The10000071? = nil,
        the10000072: The10000072? = nil,
        the10000073: The10000073? = nil,
        the10000074: The10000074? = nil,
        the10000075: The10000075? = nil,
        the10000076: The10000076? = nil,
        the10000077: The10000077? = nil,
        the10000078: The10000078? = nil,
        the10000079: The10000079? = nil,
        the10000080: The10000080? = nil,
        the10000081: The10000081? = nil,
        the10000082: The10000082? = nil,
        the10000083: The10000083? = nil,
        the10000084: The10000084? = nil,
        the10000085: The10000085? = nil,
        the10000086: The10000086? = nil,
        the10000087: The10000087? = nil,
        the10000088: The10000088? = nil,
        the10000089: The10000089? = nil,
        the10000090: The10000090? = nil,
        the10000091: The10000091? = nil,
        the10000092: The10000092? = nil,
        the10000093: The10000093? = nil,
        the10000094: The10000094? = nil,
        the10000095: The10000095? = nil,
        the10000096: The10000096? = nil,
        the10000097: The10000097? = nil,
        the10000098: The10000098? = nil,
        the10000099: The10000099? = nil,
        the10000100: The10000100? = nil,
        the10000101: The10000101? = nil,
        the10000102: The10000102? = nil,
        the10000005503: The10000005503? = nil,
        the10000005504: The10000005? = nil,
        the10000005506: The10000005506? = nil,
        the10000005507: The10000005507? = nil,
        the10000005508: The10000005508? = nil,
        the10000005501: The10000005501? = nil,
        the10000007701: The10000007701? = nil,
        the10000007703: The10000007703? = nil,
        the10000007704: The10000007? = nil,
        the10000007706: The10000007706? = nil,
        the10000007707: The10000007707? = nil,
        the10000007708: The10000007708? = nil
    ) -> CharactersJSON {
        return CharactersJSON(
            the10000002: the10000002 ?? self.the10000002,
            the10000003: the10000003 ?? self.the10000003,
            the10000005: the10000005 ?? self.the10000005,
            the10000006: the10000006 ?? self.the10000006,
            the10000007: the10000007 ?? self.the10000007,
            the10000014: the10000014 ?? self.the10000014,
            the10000015: the10000015 ?? self.the10000015,
            the10000016: the10000016 ?? self.the10000016,
            the10000020: the10000020 ?? self.the10000020,
            the10000021: the10000021 ?? self.the10000021,
            the10000022: the10000022 ?? self.the10000022,
            the10000023: the10000023 ?? self.the10000023,
            the10000024: the10000024 ?? self.the10000024,
            the10000025: the10000025 ?? self.the10000025,
            the10000026: the10000026 ?? self.the10000026,
            the10000027: the10000027 ?? self.the10000027,
            the10000029: the10000029 ?? self.the10000029,
            the10000030: the10000030 ?? self.the10000030,
            the10000031: the10000031 ?? self.the10000031,
            the10000032: the10000032 ?? self.the10000032,
            the10000033: the10000033 ?? self.the10000033,
            the10000034: the10000034 ?? self.the10000034,
            the10000035: the10000035 ?? self.the10000035,
            the10000036: the10000036 ?? self.the10000036,
            the10000037: the10000037 ?? self.the10000037,
            the10000038: the10000038 ?? self.the10000038,
            the10000039: the10000039 ?? self.the10000039,
            the10000041: the10000041 ?? self.the10000041,
            the10000042: the10000042 ?? self.the10000042,
            the10000043: the10000043 ?? self.the10000043,
            the10000044: the10000044 ?? self.the10000044,
            the10000045: the10000045 ?? self.the10000045,
            the10000046: the10000046 ?? self.the10000046,
            the10000047: the10000047 ?? self.the10000047,
            the10000048: the10000048 ?? self.the10000048,
            the10000049: the10000049 ?? self.the10000049,
            the10000050: the10000050 ?? self.the10000050,
            the10000051: the10000051 ?? self.the10000051,
            the10000052: the10000052 ?? self.the10000052,
            the10000053: the10000053 ?? self.the10000053,
            the10000054: the10000054 ?? self.the10000054,
            the10000055: the10000055 ?? self.the10000055,
            the10000056: the10000056 ?? self.the10000056,
            the10000057: the10000057 ?? self.the10000057,
            the10000058: the10000058 ?? self.the10000058,
            the10000059: the10000059 ?? self.the10000059,
            the10000060: the10000060 ?? self.the10000060,
            the10000061: the10000061 ?? self.the10000061,
            the10000062: the10000062 ?? self.the10000062,
            the10000063: the10000063 ?? self.the10000063,
            the10000064: the10000064 ?? self.the10000064,
            the10000065: the10000065 ?? self.the10000065,
            the10000066: the10000066 ?? self.the10000066,
            the10000067: the10000067 ?? self.the10000067,
            the10000068: the10000068 ?? self.the10000068,
            the10000069: the10000069 ?? self.the10000069,
            the10000070: the10000070 ?? self.the10000070,
            the10000071: the10000071 ?? self.the10000071,
            the10000072: the10000072 ?? self.the10000072,
            the10000073: the10000073 ?? self.the10000073,
            the10000074: the10000074 ?? self.the10000074,
            the10000075: the10000075 ?? self.the10000075,
            the10000076: the10000076 ?? self.the10000076,
            the10000077: the10000077 ?? self.the10000077,
            the10000078: the10000078 ?? self.the10000078,
            the10000079: the10000079 ?? self.the10000079,
            the10000080: the10000080 ?? self.the10000080,
            the10000081: the10000081 ?? self.the10000081,
            the10000082: the10000082 ?? self.the10000082,
            the10000083: the10000083 ?? self.the10000083,
            the10000084: the10000084 ?? self.the10000084,
            the10000085: the10000085 ?? self.the10000085,
            the10000086: the10000086 ?? self.the10000086,
            the10000087: the10000087 ?? self.the10000087,
            the10000088: the10000088 ?? self.the10000088,
            the10000089: the10000089 ?? self.the10000089,
            the10000090: the10000090 ?? self.the10000090,
            the10000091: the10000091 ?? self.the10000091,
            the10000092: the10000092 ?? self.the10000092,
            the10000093: the10000093 ?? self.the10000093,
            the10000094: the10000094 ?? self.the10000094,
            the10000095: the10000095 ?? self.the10000095,
            the10000096: the10000096 ?? self.the10000096,
            the10000097: the10000097 ?? self.the10000097,
            the10000098: the10000098 ?? self.the10000098,
            the10000099: the10000099 ?? self.the10000099,
            the10000100: the10000100 ?? self.the10000100,
            the10000101: the10000101 ?? self.the10000101,
            the10000102: the10000102 ?? self.the10000102,
            the10000005503: the10000005503 ?? self.the10000005503,
            the10000005504: the10000005504 ?? self.the10000005504,
            the10000005506: the10000005506 ?? self.the10000005506,
            the10000005507: the10000005507 ?? self.the10000005507,
            the10000005508: the10000005508 ?? self.the10000005508,
            the10000005501: the10000005501 ?? self.the10000005501,
            the10000007701: the10000007701 ?? self.the10000007701,
            the10000007703: the10000007703 ?? self.the10000007703,
            the10000007704: the10000007704 ?? self.the10000007704,
            the10000007706: the10000007706 ?? self.the10000007706,
            the10000007707: the10000007707 ?? self.the10000007707,
            the10000007708: the10000007708 ?? self.the10000007708
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000002
struct The10000002: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000002_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000002 convenience initializers and mutators

extension The10000002 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000002.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000002_Costumes? = nil
    ) -> The10000002 {
        return The10000002(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000002_Costumes
struct The10000002_Costumes: Codable {
    let the200201: The200201

    enum CodingKeys: String, CodingKey {
        case the200201 = "200201"
    }
}

// MARK: The10000002_Costumes convenience initializers and mutators

extension The10000002_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000002_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the200201: The200201? = nil
    ) -> The10000002_Costumes {
        return The10000002_Costumes(
            the200201: the200201 ?? self.the200201
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The200201
struct The200201: Codable {
    let sideIconName, icon, art: String
    let avatarID: Int

    enum CodingKeys: String, CodingKey {
        case sideIconName, icon, art
        case avatarID = "avatarId"
    }
}

// MARK: The200201 convenience initializers and mutators

extension The200201 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The200201.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        sideIconName: String? = nil,
        icon: String? = nil,
        art: String? = nil,
        avatarID: Int? = nil
    ) -> The200201 {
        return The200201(
            sideIconName: sideIconName ?? self.sideIconName,
            icon: icon ?? self.icon,
            art: art ?? self.art,
            avatarID: avatarID ?? self.avatarID
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000003
struct The10000003: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: [String: The200201]

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000003 convenience initializers and mutators

extension The10000003 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000003.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: [String: The200201]? = nil
    ) -> The10000003 {
        return The10000003(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000005
struct The10000005: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let nameTextMapHash: Int
    let proudMap: [String: Int]
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case nameTextMapHash = "NameTextMapHash"
        case proudMap = "ProudMap"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000005 convenience initializers and mutators

extension The10000005 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000005.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        nameTextMapHash: Int? = nil,
        proudMap: [String: Int]? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000005 {
        return The10000005(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            proudMap: proudMap ?? self.proudMap,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000005501
struct The10000005501: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let nameTextMapHash: Int
    let proudMap: [String: Int]
    let sideIconName, qualityType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case nameTextMapHash = "NameTextMapHash"
        case proudMap = "ProudMap"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
    }
}

// MARK: The10000005501 convenience initializers and mutators

extension The10000005501 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000005501.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        nameTextMapHash: Int? = nil,
        proudMap: [String: Int]? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil
    ) -> The10000005501 {
        return The10000005501(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            proudMap: proudMap ?? self.proudMap,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000005503
struct The10000005503: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let nameTextMapHash: Int
    let proudMap: [String: Int]
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case nameTextMapHash = "NameTextMapHash"
        case proudMap = "ProudMap"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000005503 convenience initializers and mutators

extension The10000005503 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000005503.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        nameTextMapHash: Int? = nil,
        proudMap: [String: Int]? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000005503 {
        return The10000005503(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            proudMap: proudMap ?? self.proudMap,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000005506
struct The10000005506: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let nameTextMapHash: Int
    let proudMap: [String: Int]
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case nameTextMapHash = "NameTextMapHash"
        case proudMap = "ProudMap"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000005506 convenience initializers and mutators

extension The10000005506 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000005506.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        nameTextMapHash: Int? = nil,
        proudMap: [String: Int]? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000005506 {
        return The10000005506(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            proudMap: proudMap ?? self.proudMap,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000005507
struct The10000005507: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let nameTextMapHash: Int
    let proudMap: [String: Int]
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case nameTextMapHash = "NameTextMapHash"
        case proudMap = "ProudMap"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000005507 convenience initializers and mutators

extension The10000005507 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000005507.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        nameTextMapHash: Int? = nil,
        proudMap: [String: Int]? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000005507 {
        return The10000005507(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            proudMap: proudMap ?? self.proudMap,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000005508
struct The10000005508: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let nameTextMapHash: Int
    let proudMap: [String: Int]
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case nameTextMapHash = "NameTextMapHash"
        case proudMap = "ProudMap"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000005508 convenience initializers and mutators

extension The10000005508 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000005508.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        nameTextMapHash: Int? = nil,
        proudMap: [String: Int]? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000005508 {
        return The10000005508(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            proudMap: proudMap ?? self.proudMap,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000006
struct The10000006: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000006_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000006 convenience initializers and mutators

extension The10000006 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000006.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000006_Costumes? = nil
    ) -> The10000006 {
        return The10000006(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000006_Costumes
struct The10000006_Costumes: Codable {
    let the200601: The200201

    enum CodingKeys: String, CodingKey {
        case the200601 = "200601"
    }
}

// MARK: The10000006_Costumes convenience initializers and mutators

extension The10000006_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000006_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the200601: The200201? = nil
    ) -> The10000006_Costumes {
        return The10000006_Costumes(
            the200601: the200601 ?? self.the200601
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000007
struct The10000007: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let nameTextMapHash: Int
    let proudMap: [String: Int]
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case nameTextMapHash = "NameTextMapHash"
        case proudMap = "ProudMap"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000007 convenience initializers and mutators

extension The10000007 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000007.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        nameTextMapHash: Int? = nil,
        proudMap: [String: Int]? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000007 {
        return The10000007(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            proudMap: proudMap ?? self.proudMap,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000007701
struct The10000007701: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let nameTextMapHash: Int
    let proudMap: [String: Int]
    let sideIconName, qualityType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case nameTextMapHash = "NameTextMapHash"
        case proudMap = "ProudMap"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
    }
}

// MARK: The10000007701 convenience initializers and mutators

extension The10000007701 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000007701.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        nameTextMapHash: Int? = nil,
        proudMap: [String: Int]? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil
    ) -> The10000007701 {
        return The10000007701(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            proudMap: proudMap ?? self.proudMap,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000007703
struct The10000007703: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let nameTextMapHash: Int
    let proudMap: [String: Int]
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case nameTextMapHash = "NameTextMapHash"
        case proudMap = "ProudMap"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000007703 convenience initializers and mutators

extension The10000007703 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000007703.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        nameTextMapHash: Int? = nil,
        proudMap: [String: Int]? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000007703 {
        return The10000007703(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            proudMap: proudMap ?? self.proudMap,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000007706
struct The10000007706: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let nameTextMapHash: Int
    let proudMap: [String: Int]
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case nameTextMapHash = "NameTextMapHash"
        case proudMap = "ProudMap"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000007706 convenience initializers and mutators

extension The10000007706 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000007706.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        nameTextMapHash: Int? = nil,
        proudMap: [String: Int]? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000007706 {
        return The10000007706(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            proudMap: proudMap ?? self.proudMap,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000007707
struct The10000007707: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let nameTextMapHash: Int
    let proudMap: [String: Int]
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case nameTextMapHash = "NameTextMapHash"
        case proudMap = "ProudMap"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000007707 convenience initializers and mutators

extension The10000007707 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000007707.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        nameTextMapHash: Int? = nil,
        proudMap: [String: Int]? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000007707 {
        return The10000007707(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            proudMap: proudMap ?? self.proudMap,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000007708
struct The10000007708: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let nameTextMapHash: Int
    let proudMap: [String: Int]
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case nameTextMapHash = "NameTextMapHash"
        case proudMap = "ProudMap"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000007708 convenience initializers and mutators

extension The10000007708 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000007708.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        nameTextMapHash: Int? = nil,
        proudMap: [String: Int]? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000007708 {
        return The10000007708(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            proudMap: proudMap ?? self.proudMap,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000014
struct The10000014: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000014_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000014 convenience initializers and mutators

extension The10000014 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000014.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000014_Costumes? = nil
    ) -> The10000014 {
        return The10000014(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000014_Costumes
struct The10000014_Costumes: Codable {
    let the201401: The200201

    enum CodingKeys: String, CodingKey {
        case the201401 = "201401"
    }
}

// MARK: The10000014_Costumes convenience initializers and mutators

extension The10000014_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000014_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the201401: The200201? = nil
    ) -> The10000014_Costumes {
        return The10000014_Costumes(
            the201401: the201401 ?? self.the201401
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000015
struct The10000015: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000015_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000015 convenience initializers and mutators

extension The10000015 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000015.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000015_Costumes? = nil
    ) -> The10000015 {
        return The10000015(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000015_Costumes
struct The10000015_Costumes: Codable {
    let the201501: The200201

    enum CodingKeys: String, CodingKey {
        case the201501 = "201501"
    }
}

// MARK: The10000015_Costumes convenience initializers and mutators

extension The10000015_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000015_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the201501: The200201? = nil
    ) -> The10000015_Costumes {
        return The10000015_Costumes(
            the201501: the201501 ?? self.the201501
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000016
struct The10000016: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000016_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000016 convenience initializers and mutators

extension The10000016 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000016.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000016_Costumes? = nil
    ) -> The10000016 {
        return The10000016(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000016_Costumes
struct The10000016_Costumes: Codable {
    let the201601: The200201

    enum CodingKeys: String, CodingKey {
        case the201601 = "201601"
    }
}

// MARK: The10000016_Costumes convenience initializers and mutators

extension The10000016_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000016_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the201601: The200201? = nil
    ) -> The10000016_Costumes {
        return The10000016_Costumes(
            the201601: the201601 ?? self.the201601
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000020
struct The10000020: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000020 convenience initializers and mutators

extension The10000020 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000020.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000020 {
        return The10000020(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000021
struct The10000021: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000021_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000021 convenience initializers and mutators

extension The10000021 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000021.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000021_Costumes? = nil
    ) -> The10000021 {
        return The10000021(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000021_Costumes
struct The10000021_Costumes: Codable {
    let the202101: The200201

    enum CodingKeys: String, CodingKey {
        case the202101 = "202101"
    }
}

// MARK: The10000021_Costumes convenience initializers and mutators

extension The10000021_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000021_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the202101: The200201? = nil
    ) -> The10000021_Costumes {
        return The10000021_Costumes(
            the202101: the202101 ?? self.the202101
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000022
struct The10000022: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000022 convenience initializers and mutators

extension The10000022 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000022.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000022 {
        return The10000022(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000023
struct The10000023: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000023 convenience initializers and mutators

extension The10000023 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000023.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000023 {
        return The10000023(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000024
struct The10000024: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000024 convenience initializers and mutators

extension The10000024 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000024.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000024 {
        return The10000024(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000025
struct The10000025: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000025_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000025 convenience initializers and mutators

extension The10000025 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000025.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000025_Costumes? = nil
    ) -> The10000025 {
        return The10000025(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000025_Costumes
struct The10000025_Costumes: Codable {
    let the202501: The200201

    enum CodingKeys: String, CodingKey {
        case the202501 = "202501"
    }
}

// MARK: The10000025_Costumes convenience initializers and mutators

extension The10000025_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000025_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the202501: The200201? = nil
    ) -> The10000025_Costumes {
        return The10000025_Costumes(
            the202501: the202501 ?? self.the202501
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000026
struct The10000026: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000026 convenience initializers and mutators

extension The10000026 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000026.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000026 {
        return The10000026(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000027
struct The10000027: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000027_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000027 convenience initializers and mutators

extension The10000027 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000027.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000027_Costumes? = nil
    ) -> The10000027 {
        return The10000027(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000027_Costumes
struct The10000027_Costumes: Codable {
    let the202701: The200201

    enum CodingKeys: String, CodingKey {
        case the202701 = "202701"
    }
}

// MARK: The10000027_Costumes convenience initializers and mutators

extension The10000027_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000027_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the202701: The200201? = nil
    ) -> The10000027_Costumes {
        return The10000027_Costumes(
            the202701: the202701 ?? self.the202701
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000029
struct The10000029: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000029_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000029 convenience initializers and mutators

extension The10000029 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000029.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000029_Costumes? = nil
    ) -> The10000029 {
        return The10000029(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000029_Costumes
struct The10000029_Costumes: Codable {
    let the202901: The200201

    enum CodingKeys: String, CodingKey {
        case the202901 = "202901"
    }
}

// MARK: The10000029_Costumes convenience initializers and mutators

extension The10000029_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000029_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the202901: The200201? = nil
    ) -> The10000029_Costumes {
        return The10000029_Costumes(
            the202901: the202901 ?? self.the202901
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000030
struct The10000030: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000030 convenience initializers and mutators

extension The10000030 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000030.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000030 {
        return The10000030(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000031
struct The10000031: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000031_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000031 convenience initializers and mutators

extension The10000031 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000031.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000031_Costumes? = nil
    ) -> The10000031 {
        return The10000031(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000031_Costumes
struct The10000031_Costumes: Codable {
    let the203101: The200201

    enum CodingKeys: String, CodingKey {
        case the203101 = "203101"
    }
}

// MARK: The10000031_Costumes convenience initializers and mutators

extension The10000031_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000031_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the203101: The200201? = nil
    ) -> The10000031_Costumes {
        return The10000031_Costumes(
            the203101: the203101 ?? self.the203101
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000032
struct The10000032: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000032 convenience initializers and mutators

extension The10000032 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000032.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000032 {
        return The10000032(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000033
struct The10000033: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000033 convenience initializers and mutators

extension The10000033 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000033.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000033 {
        return The10000033(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000034
struct The10000034: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000034 convenience initializers and mutators

extension The10000034 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000034.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000034 {
        return The10000034(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000035
struct The10000035: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000035 convenience initializers and mutators

extension The10000035 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000035.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000035 {
        return The10000035(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000036
struct The10000036: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000036 convenience initializers and mutators

extension The10000036 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000036.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000036 {
        return The10000036(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000037
struct The10000037: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000037_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000037 convenience initializers and mutators

extension The10000037 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000037.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000037_Costumes? = nil
    ) -> The10000037 {
        return The10000037(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000037_Costumes
struct The10000037_Costumes: Codable {
    let the203701: The200201

    enum CodingKeys: String, CodingKey {
        case the203701 = "203701"
    }
}

// MARK: The10000037_Costumes convenience initializers and mutators

extension The10000037_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000037_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the203701: The200201? = nil
    ) -> The10000037_Costumes {
        return The10000037_Costumes(
            the203701: the203701 ?? self.the203701
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000038
struct The10000038: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000038 convenience initializers and mutators

extension The10000038 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000038.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000038 {
        return The10000038(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000039
struct The10000039: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000039 convenience initializers and mutators

extension The10000039 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000039.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000039 {
        return The10000039(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000041
struct The10000041: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000041_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000041 convenience initializers and mutators

extension The10000041 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000041.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000041_Costumes? = nil
    ) -> The10000041 {
        return The10000041(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000041_Costumes
struct The10000041_Costumes: Codable {
    let the204101: The200201

    enum CodingKeys: String, CodingKey {
        case the204101 = "204101"
    }
}

// MARK: The10000041_Costumes convenience initializers and mutators

extension The10000041_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000041_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the204101: The200201? = nil
    ) -> The10000041_Costumes {
        return The10000041_Costumes(
            the204101: the204101 ?? self.the204101
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000042
struct The10000042: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000042_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000042 convenience initializers and mutators

extension The10000042 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000042.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000042_Costumes? = nil
    ) -> The10000042 {
        return The10000042(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000042_Costumes
struct The10000042_Costumes: Codable {
    let the204201: The200201

    enum CodingKeys: String, CodingKey {
        case the204201 = "204201"
    }
}

// MARK: The10000042_Costumes convenience initializers and mutators

extension The10000042_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000042_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the204201: The200201? = nil
    ) -> The10000042_Costumes {
        return The10000042_Costumes(
            the204201: the204201 ?? self.the204201
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000043
struct The10000043: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000043 convenience initializers and mutators

extension The10000043 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000043.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000043 {
        return The10000043(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000044
struct The10000044: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000044 convenience initializers and mutators

extension The10000044 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000044.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000044 {
        return The10000044(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000045
struct The10000045: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000045_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000045 convenience initializers and mutators

extension The10000045 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000045.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000045_Costumes? = nil
    ) -> The10000045 {
        return The10000045(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000045_Costumes
struct The10000045_Costumes: Codable {
    let the204501: The200201

    enum CodingKeys: String, CodingKey {
        case the204501 = "204501"
    }
}

// MARK: The10000045_Costumes convenience initializers and mutators

extension The10000045_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000045_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the204501: The200201? = nil
    ) -> The10000045_Costumes {
        return The10000045_Costumes(
            the204501: the204501 ?? self.the204501
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000046
struct The10000046: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000046 convenience initializers and mutators

extension The10000046 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000046.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000046 {
        return The10000046(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000047
struct The10000047: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000047 convenience initializers and mutators

extension The10000047 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000047.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000047 {
        return The10000047(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000048
struct The10000048: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000048 convenience initializers and mutators

extension The10000048 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000048.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000048 {
        return The10000048(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000049
struct The10000049: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000049 convenience initializers and mutators

extension The10000049 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000049.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000049 {
        return The10000049(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000050
struct The10000050: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000050 convenience initializers and mutators

extension The10000050 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000050.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000050 {
        return The10000050(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000051
struct The10000051: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000051 convenience initializers and mutators

extension The10000051 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000051.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000051 {
        return The10000051(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000052
struct The10000052: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000052 convenience initializers and mutators

extension The10000052 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000052.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000052 {
        return The10000052(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000053
struct The10000053: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000053 convenience initializers and mutators

extension The10000053 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000053.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000053 {
        return The10000053(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000054
struct The10000054: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000054 convenience initializers and mutators

extension The10000054 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000054.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000054 {
        return The10000054(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000055
struct The10000055: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000055 convenience initializers and mutators

extension The10000055 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000055.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000055 {
        return The10000055(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000056
struct The10000056: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000056 convenience initializers and mutators

extension The10000056 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000056.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000056 {
        return The10000056(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000057
struct The10000057: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000057 convenience initializers and mutators

extension The10000057 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000057.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000057 {
        return The10000057(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000058
struct The10000058: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000058 convenience initializers and mutators

extension The10000058 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000058.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000058 {
        return The10000058(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000059
struct The10000059: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000059 convenience initializers and mutators

extension The10000059 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000059.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000059 {
        return The10000059(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000060
struct The10000060: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000060 convenience initializers and mutators

extension The10000060 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000060.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000060 {
        return The10000060(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000061
struct The10000061: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000061_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000061 convenience initializers and mutators

extension The10000061 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000061.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000061_Costumes? = nil
    ) -> The10000061 {
        return The10000061(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000061_Costumes
struct The10000061_Costumes: Codable {
    let the206101: The200201

    enum CodingKeys: String, CodingKey {
        case the206101 = "206101"
    }
}

// MARK: The10000061_Costumes convenience initializers and mutators

extension The10000061_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000061_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the206101: The200201? = nil
    ) -> The10000061_Costumes {
        return The10000061_Costumes(
            the206101: the206101 ?? self.the206101
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000062
struct The10000062: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000062 convenience initializers and mutators

extension The10000062 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000062.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000062 {
        return The10000062(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000063
struct The10000063: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000063_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000063 convenience initializers and mutators

extension The10000063 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000063.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000063_Costumes? = nil
    ) -> The10000063 {
        return The10000063(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000063_Costumes
struct The10000063_Costumes: Codable {
    let the206301: The200201

    enum CodingKeys: String, CodingKey {
        case the206301 = "206301"
    }
}

// MARK: The10000063_Costumes convenience initializers and mutators

extension The10000063_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000063_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the206301: The200201? = nil
    ) -> The10000063_Costumes {
        return The10000063_Costumes(
            the206301: the206301 ?? self.the206301
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000064
struct The10000064: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000064 convenience initializers and mutators

extension The10000064 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000064.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000064 {
        return The10000064(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000065
struct The10000065: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000065 convenience initializers and mutators

extension The10000065 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000065.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000065 {
        return The10000065(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000066
struct The10000066: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000066 convenience initializers and mutators

extension The10000066 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000066.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000066 {
        return The10000066(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000067
struct The10000067: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000067 convenience initializers and mutators

extension The10000067 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000067.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000067 {
        return The10000067(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000068
struct The10000068: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000068 convenience initializers and mutators

extension The10000068 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000068.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000068 {
        return The10000068(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000069
struct The10000069: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000069 convenience initializers and mutators

extension The10000069 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000069.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000069 {
        return The10000069(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000070
struct The10000070: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String
    let costumes: The10000070_Costumes

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
        case costumes = "Costumes"
    }
}

// MARK: The10000070 convenience initializers and mutators

extension The10000070 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000070.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil,
        costumes: The10000070_Costumes? = nil
    ) -> The10000070 {
        return The10000070(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType,
            costumes: costumes ?? self.costumes
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000070_Costumes
struct The10000070_Costumes: Codable {
    let the207001: The200201

    enum CodingKeys: String, CodingKey {
        case the207001 = "207001"
    }
}

// MARK: The10000070_Costumes convenience initializers and mutators

extension The10000070_Costumes {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000070_Costumes.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        the207001: The200201? = nil
    ) -> The10000070_Costumes {
        return The10000070_Costumes(
            the207001: the207001 ?? self.the207001
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000071
struct The10000071: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000071 convenience initializers and mutators

extension The10000071 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000071.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000071 {
        return The10000071(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000072
struct The10000072: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000072 convenience initializers and mutators

extension The10000072 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000072.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000072 {
        return The10000072(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000073
struct The10000073: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000073 convenience initializers and mutators

extension The10000073 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000073.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000073 {
        return The10000073(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000074
struct The10000074: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000074 convenience initializers and mutators

extension The10000074 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000074.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000074 {
        return The10000074(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000075
struct The10000075: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000075 convenience initializers and mutators

extension The10000075 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000075.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000075 {
        return The10000075(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000076
struct The10000076: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000076 convenience initializers and mutators

extension The10000076 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000076.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000076 {
        return The10000076(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000077
struct The10000077: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000077 convenience initializers and mutators

extension The10000077 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000077.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000077 {
        return The10000077(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000078
struct The10000078: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000078 convenience initializers and mutators

extension The10000078 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000078.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000078 {
        return The10000078(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000079
struct The10000079: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000079 convenience initializers and mutators

extension The10000079 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000079.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000079 {
        return The10000079(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000080
struct The10000080: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000080 convenience initializers and mutators

extension The10000080 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000080.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000080 {
        return The10000080(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000081
struct The10000081: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000081 convenience initializers and mutators

extension The10000081 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000081.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000081 {
        return The10000081(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000082
struct The10000082: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000082 convenience initializers and mutators

extension The10000082 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000082.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000082 {
        return The10000082(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000083
struct The10000083: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000083 convenience initializers and mutators

extension The10000083 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000083.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000083 {
        return The10000083(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000084
struct The10000084: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000084 convenience initializers and mutators

extension The10000084 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000084.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000084 {
        return The10000084(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000085
struct The10000085: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000085 convenience initializers and mutators

extension The10000085 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000085.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000085 {
        return The10000085(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000086
struct The10000086: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000086 convenience initializers and mutators

extension The10000086 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000086.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000086 {
        return The10000086(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000087
struct The10000087: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000087 convenience initializers and mutators

extension The10000087 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000087.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000087 {
        return The10000087(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000088
struct The10000088: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000088 convenience initializers and mutators

extension The10000088 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000088.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000088 {
        return The10000088(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000089
struct The10000089: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000089 convenience initializers and mutators

extension The10000089 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000089.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000089 {
        return The10000089(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000090
struct The10000090: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000090 convenience initializers and mutators

extension The10000090 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000090.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000090 {
        return The10000090(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000091
struct The10000091: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000091 convenience initializers and mutators

extension The10000091 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000091.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000091 {
        return The10000091(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000092
struct The10000092: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000092 convenience initializers and mutators

extension The10000092 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000092.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000092 {
        return The10000092(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000093
struct The10000093: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000093 convenience initializers and mutators

extension The10000093 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000093.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000093 {
        return The10000093(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000094
struct The10000094: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000094 convenience initializers and mutators

extension The10000094 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000094.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000094 {
        return The10000094(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000095
struct The10000095: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000095 convenience initializers and mutators

extension The10000095 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000095.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000095 {
        return The10000095(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000096
struct The10000096: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000096 convenience initializers and mutators

extension The10000096 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000096.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000096 {
        return The10000096(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000097
struct The10000097: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000097 convenience initializers and mutators

extension The10000097 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000097.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000097 {
        return The10000097(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000098
struct The10000098: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000098 convenience initializers and mutators

extension The10000098 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000098.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000098 {
        return The10000098(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000099
struct The10000099: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000099 convenience initializers and mutators

extension The10000099 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000099.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000099 {
        return The10000099(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000100
struct The10000100: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000100 convenience initializers and mutators

extension The10000100 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000100.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000100 {
        return The10000100(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000101
struct The10000101: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000101 convenience initializers and mutators

extension The10000101 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000101.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000101 {
        return The10000101(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - The10000102
struct The10000102: Codable {
    let element: String
    let consts: [String]
    let skillOrder: [Int]
    let skills: [String: String]
    let proudMap: [String: Int]
    let nameTextMapHash: Int
    let sideIconName, qualityType, weaponType: String

    enum CodingKeys: String, CodingKey {
        case element = "Element"
        case consts = "Consts"
        case skillOrder = "SkillOrder"
        case skills = "Skills"
        case proudMap = "ProudMap"
        case nameTextMapHash = "NameTextMapHash"
        case sideIconName = "SideIconName"
        case qualityType = "QualityType"
        case weaponType = "WeaponType"
    }
}

// MARK: The10000102 convenience initializers and mutators

extension The10000102 {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(The10000102.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        element: String? = nil,
        consts: [String]? = nil,
        skillOrder: [Int]? = nil,
        skills: [String: String]? = nil,
        proudMap: [String: Int]? = nil,
        nameTextMapHash: Int? = nil,
        sideIconName: String? = nil,
        qualityType: String? = nil,
        weaponType: String? = nil
    ) -> The10000102 {
        return The10000102(
            element: element ?? self.element,
            consts: consts ?? self.consts,
            skillOrder: skillOrder ?? self.skillOrder,
            skills: skills ?? self.skills,
            proudMap: proudMap ?? self.proudMap,
            nameTextMapHash: nameTextMapHash ?? self.nameTextMapHash,
            sideIconName: sideIconName ?? self.sideIconName,
            qualityType: qualityType ?? self.qualityType,
            weaponType: weaponType ?? self.weaponType
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - Helper functions for creating encoders and decoders

func newJSONDecoder() -> JSONDecoder {
    let decoder = JSONDecoder()
    if #available(iOS 10.0, OSX 10.12, tvOS 10.0, watchOS 3.0, *) {
        decoder.dateDecodingStrategy = .iso8601
    }
    return decoder
}

func newJSONEncoder() -> JSONEncoder {
    let encoder = JSONEncoder()
    if #available(iOS 10.0, OSX 10.12, tvOS 10.0, watchOS 3.0, *) {
        encoder.dateEncodingStrategy = .iso8601
    }
    return encoder
}
